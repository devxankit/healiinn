const asyncHandler = require('../../middleware/asyncHandler');
const Appointment = require('../../models/Appointment');

// GET /api/doctors/appointments (already in dashboard controller, but keeping for consistency)
// This is handled by doctorDashboardController.js

